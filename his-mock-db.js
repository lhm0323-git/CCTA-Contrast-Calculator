/**
 * NOTE: Mock HIS Patient Database has been bundled directly inside `app.js`
 * to support opening `index.html` via the local `file:///` protocol (preventing CORS block errors).
 * 
 * Please edit the mock database directly inside `app.js` if modifications are needed.
 */
